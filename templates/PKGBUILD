# Maintainer: IGN-STYLY on github.com <suporte.lazar@gmail.com>
pkgname= pkg-replace
pkgver=0.1
pkgrel=1
pkgdesc="BETA"
arch=(x86_64)
url="url-replace"
license=('MIT')
depends=()
makedepends=()
checkdepends=()
provides=(lotus)
source=("git+$url")
install=
noextract=()


build() {
	./$pkgname/make.sh $pkgname
}

package() {
	mkdir -p $pkgdir/usr/bin/
	cp ./build/bin/$pkgname $pkgdir/usr/bin/lotus
	
}
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
sha256sums=('SKIP')
